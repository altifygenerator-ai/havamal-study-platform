"use client";
import { FormEvent,useEffect,useMemo,useState } from "react";
import { useRouter } from "next/navigation";
import { createSupabaseBrowserClient } from "@/lib/supabase/client";
export function AccountManager(){const db=useMemo(()=>createSupabaseBrowserClient(),[]),router=useRouter(),[userId,setUserId]=useState<string|null>(null),[email,setEmail]=useState(""),[displayName,setDisplayName]=useState(""),[bio,setBio]=useState(""),[message,setMessage]=useState("");useEffect(()=>{if(!db)return;db.auth.getUser().then(async({data})=>{if(!data.user)return;setUserId(data.user.id);setEmail(data.user.email||"");const{data:profile}=await db.from("profiles").select("display_name,bio").eq("id",data.user.id).maybeSingle();setDisplayName(profile?.display_name||"");setBio(profile?.bio||"")})},[db]);async function save(e:FormEvent){e.preventDefault();if(!db||!userId)return;const{error}=await db.from("profiles").update({display_name:displayName.trim(),bio:bio.trim()||null}).eq("id",userId);setMessage(error?.message||"Profile saved.")}async function signOut(){await db?.auth.signOut();setUserId(null);router.refresh()}async function exportData(){const response=await fetch("/api/account/export");if(!response.ok){setMessage((await response.json()).error);return}const blob=await response.blob(),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url;a.download=response.headers.get("content-disposition")?.match(/filename="([^"]+)"/)?.[1]||"havamal-study-data.json";a.click();URL.revokeObjectURL(url)}async function remove(){if(!confirm("Delete this account and all private study data? This cannot be undone."))return;const response=await fetch("/api/account/delete",{method:"POST"});const result=await response.json();if(!response.ok){setMessage(result.error);return}setUserId(null);router.push("/");router.refresh()}if(!userId)return null;return <section className="account-manager"><div className="section-kicker">Signed in</div><h2>{email}</h2><form className="form-stack" onSubmit={save}><label>Display name<input value={displayName} maxLength={80} onChange={e=>setDisplayName(e.target.value)} required/></label><label>Short bio<textarea value={bio} maxLength={500} onChange={e=>setBio(e.target.value)}/></label><button className="button">Save profile</button></form><div className="account-actions"><button className="button button-secondary" onClick={exportData}>Export my data</button><button className="button button-secondary" onClick={signOut}>Sign out</button><button className="danger-button" onClick={remove}>Delete account</button></div><p aria-live="polite">{message}</p></section>}
