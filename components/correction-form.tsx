"use client";
import { FormEvent,useState } from "react";
export function CorrectionForm(){const[message,setMessage]=useState("");async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const form=e.currentTarget;const payload=Object.fromEntries(new FormData(form));const response=await fetch("/api/corrections",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const result=await response.json();setMessage(response.ok?"The correction report was submitted for review.":result.error);if(response.ok)form.reset()}return <form className="form-stack" onSubmit={submit}><label>Issue type<select name="category"><option value="transcription_error">Transcription error</option><option value="numbering_mismatch">Stanza-number mismatch</option><option value="broken_character">Broken character</option><option value="missing_line">Missing line</option><option value="incorrect_attribution">Incorrect attribution</option><option value="licensing_concern">Licensing concern</option><option value="broken_source_link">Broken source link</option><option value="metadata_issue">Metadata issue</option></select></label><label>Affected source<input name="affectedSource" required/></label><label>Affected passage<input name="affectedPassage"/></label><label>Description<textarea name="description" minLength={10} required/></label><label>Suggested correction<textarea name="suggestedCorrection"/></label><label>Supporting source URL<input name="supportingSource" type="url" required/></label><label>Contact email (optional)<input name="contactEmail" type="email"/></label><button className="button">Submit correction</button><p aria-live="polite">{message}</p></form>}
