"use client";
import { FormEvent,useState } from "react";
import { useRouter } from "next/navigation";
export function NewThreadForm({categorySlug}:{categorySlug:string}){const router=useRouter(),[title,setTitle]=useState(""),[body,setBody]=useState(""),[message,setMessage]=useState("");async function submit(e:FormEvent){e.preventDefault();setMessage("");const response=await fetch("/api/forum/threads",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({categorySlug,title,body})});const result=await response.json();if(!response.ok){setMessage(result.error);return}if(result.pending)setMessage("Your opening post is awaiting moderator review. It will be listed publicly after approval.");else router.push(`/discuss/thread/${result.slug}`);router.refresh()}return <form className="discussion-composer" onSubmit={submit}><h2>Start a discussion</h2><label>Thread title<input value={title} minLength={5} maxLength={160} onChange={e=>setTitle(e.target.value)} required/></label><label>Opening post<textarea value={body} minLength={3} maxLength={12000} onChange={e=>setBody(e.target.value)} required/></label><div><p aria-live="polite">{message}</p><button>Publish thread</button></div></form>}
