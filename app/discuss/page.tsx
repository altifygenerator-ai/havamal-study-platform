import Link from "next/link";
import { categories,getAllPassages } from "@/lib/data";
export const metadata={title:"Reader discussion"};
export default function Page(){const passages=getAllPassages();return <div className="page-shell"><header className="page-heading"><h1>Reader discussion</h1><p>A source-aware community space for questions, comparison, historical context, practice, and personal reflection.</p></header><section className="notice"><strong>Discussion is not published commentary.</strong> Posts represent individual readers. Cite sources when presenting factual claims and keep disagreement focused on ideas.</section><h2 className="rule-heading">Categories</h2><div className="card-list">{categories.map(c=><article className="flat-card" key={c.slug}><h2>{c.title}</h2><p>{c.description}</p><Link href={`/discuss/category/${c.slug}`}>Open category</Link></article>)}</div><h2 className="rule-heading">Stanza-linked threads</h2><div className="passage-index">{passages.map(p=>{const x=p.editions[0];return <Link className="passage-index-row" href={`/havamal/stanza/${p.slug}#reader-discussion-heading`} key={p.slug}><span>{p.internalReference}</span><strong>{x.edition.translator} stanza {x.passage.source_stanza_number}</strong><span>Read attached discussion</span></Link>})}</div></div>}
